<span class="fa fa-star" aria-hidden="true"></span>

<% if policy(Favorite.new).create? %>
  <div class="favorite">
    <% if favorite = current_user.favorited(post) %>
      <%= link_to [post, favorite], class: 'btn btn-danger', method: :delete do %>
        <i class="fa fa-star-o"> </i>&nbsp; Unfavorite
      <% end %>
    <% else %>
      <%= link_to [post, Favorite.new], class: 'btn btn-primary', method: :post do %>
        <i class="fa fa-star"> </i>&nbsp; Favorite
      <% end %>
    <% end %>
  </div>
<% end %>

<!-- How come I can't toggle between favorite/unfavorite?? I can favorite then unfavorite but can't favorite again.. -->